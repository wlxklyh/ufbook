# GitHub Pages 部署超时问题排查指南

## 🔴 问题描述

GitHub Actions 部署到 GitHub Pages 时出现超时错误：
```
Getting Pages deployment status...
Current status: deployment_queued
Error: Timeout reached, aborting!
Canceling Pages deployment...
```

## 🔍 问题原因

`actions/deploy-pages@v4` 操作会等待部署完成，但部署状态一直停留在 `deployment_queued`，导致超时。常见原因包括：

1. **GitHub Pages 源配置错误** - 最常见的原因
2. **权限或环境配置问题**
3. **GitHub 部署队列拥堵**
4. **首次部署需要更长时间**

## ✅ 解决方案

### 方案 1：检查并修复 GitHub Pages 源配置（最重要！）

1. **访问仓库设置**
   - 打开：`https://github.com/wlxklyh/ufbook/settings/pages`

2. **检查 Pages 源设置**
   - 在 **"Build and deployment"** 部分
   - **Source** 必须选择：**`GitHub Actions`** ✅
   - **不能选择**：`Deploy from a branch` ❌

3. **如果源设置错误**
   - 将 Source 改为 `GitHub Actions`
   - 点击 **Save** 保存
   - 重新触发工作流

### 方案 2：检查环境权限配置

1. **检查环境设置**
   - 访问：`https://github.com/wlxklyh/ufbook/settings/environments`
   - 确保存在 `github-pages` 环境
   - 如果没有，GitHub Actions 会在首次运行时自动创建

2. **检查环境保护规则**
   - 如果环境有保护规则（如需要审批），可能导致部署卡住
   - 暂时禁用保护规则进行测试

### 方案 3：等待并重试

1. **首次部署**
   - 首次使用 GitHub Actions 部署可能需要 10-15 分钟
   - 如果超时时间不够，可以等待一段时间后重试

2. **GitHub 服务状态**
   - 检查 GitHub 状态页面：https://www.githubstatus.com/
   - 如果 GitHub Pages 服务有问题，需要等待服务恢复

### 方案 4：使用 gh-pages 分支部署（临时方案）

如果 GitHub Actions 持续超时，可以临时切换回 `gh-pages` 分支部署：

1. **在 GitHub 设置中切换源**
   - Settings → Pages → Source: 改为 `Deploy from a branch`
   - Branch: 选择 `gh-pages` 分支

2. **手动部署**
   ```bash
   mkdocs gh-deploy --clean
   ```

## 🔧 验证步骤

执行以下步骤验证配置是否正确：

### 步骤 1：检查工作流文件
确保 `.github/workflows/gh-pages.yml` 文件存在且配置正确。

### 步骤 2：检查 Pages 源设置
```bash
# 访问以下 URL 检查设置
https://github.com/wlxklyh/ufbook/settings/pages
```
- ✅ Source 应为：`GitHub Actions`
- ❌ Source 不应为：`Deploy from a branch`

### 步骤 3：重新触发工作流
1. 访问：`https://github.com/wlxklyh/ufbook/actions`
2. 选择 "Deploy to GitHub Pages" 工作流
3. 点击 "Run workflow" 手动触发
4. 观察部署过程

### 步骤 4：查看部署日志
在工作流运行页面查看详细日志，查找错误信息。

## 📋 常见问题

### Q1: 部署一直显示 "deployment_queued" 状态
**A:** 这通常表示 GitHub Pages 源配置错误。确保在仓库设置中选择 `GitHub Actions` 作为源。

### Q2: 工作流成功完成但网站未更新
**A:** 
- 检查 Pages 源设置是否正确
- 等待 5-10 分钟让 GitHub 完成部署
- 清除浏览器缓存后重新访问

### Q3: 权限错误
**A:** 确保工作流文件中的权限设置正确：
```yaml
permissions:
  contents: read
  pages: write
  id-token: write
```

### Q4: 是否可以增加超时时间？
**A:** `actions/deploy-pages@v4` 操作没有可配置的超时参数。如果持续超时，通常是配置问题，而不是超时时间问题。

## 🚀 推荐操作流程

1. ✅ **首先检查 Pages 源设置**（最重要）
   - 访问仓库设置页面
   - 确保 Source = `GitHub Actions`

2. ✅ **重新触发工作流**
   - 手动触发或推送到 main 分支

3. ✅ **等待部署完成**
   - 首次部署可能需要 10-15 分钟
   - 查看 Actions 标签页的实时日志

4. ✅ **验证部署结果**
   - 访问：`https://wlxklyh.github.io/ufbook/`
   - 检查网站是否正常显示

## 📚 相关文档

- [GitHub Pages 配置说明](./GITHUB_PAGES_CONFIG.md)
- [GitHub Pages 部署指南](./GITHUB_PAGES_DEPLOY.md)
- [GitHub Actions 官方文档](https://docs.github.com/en/actions)
- [GitHub Pages 官方文档](https://docs.github.com/en/pages)

---

**最后更新**: 2025-01-13

