# GitBook 脚本快速参考

## 🚀 最常用的3个命令

```bash
# 1. 快速菜单（推荐新手）
开始.bat
# 或
scripts\menu.bat

# 2. 本地预览（写作时）
scripts\serve.bat

# 3. 更新推送（完成后）
scripts\update.bat
```

---

## 📋 完整命令列表

| 脚本 | 功能 | 使用频率 |
|------|------|---------|
| `开始.bat` | 快捷菜单入口 | ⭐⭐⭐⭐⭐ |
| `scripts\install.bat` | 安装依赖（首次） | ⭐ |
| `scripts\serve.bat` | 本地预览 | ⭐⭐⭐⭐⭐ |
| `scripts\build.bat` | 构建网站 | ⭐⭐⭐ |
| `scripts\pdf.bat` | 生成PDF | ⭐⭐ |
| `scripts\update.bat` | 提交推送 | ⭐⭐⭐⭐⭐ |
| `scripts\deploy.bat` | 部署Pages | ⭐⭐ |
| `scripts\release.bat` | 创建Release | ⭐ |
| `scripts\stats.bat` | 查看统计 | ⭐⭐ |
| `scripts\clean.bat` | 清理文件 | ⭐ |

---

## 🔄 常用工作流

### 📝 日常写作流程
```bash
1. scripts\serve.bat      # 启动预览
2. [编辑 Markdown 文件]
3. [浏览器查看效果]
4. scripts\update.bat     # 推送更新
```

### 🎉 发布新版本
```bash
1. scripts\build.bat      # 测试构建
2. scripts\pdf.bat        # 生成PDF
3. scripts\release.bat    # 发布Release
4. scripts\deploy.bat     # 部署在线版
```

---

## 💡 快捷键提示

**在 Windows 资源管理器中**：
- 在项目目录按住 `Shift + 右键` → "在此处打开 PowerShell 窗口"
- 直接双击 `开始.bat` 即可启动菜单

**在命令行中**：
- 按 `Tab` 键可以自动补全路径
- 按 `↑` `↓` 键可以查看历史命令

---

## ❓ 常见问题

**Q: 如何快速开始？**
```bash
双击 开始.bat → 选择 [1] 安装依赖 → 选择 [2] 本地预览
```

**Q: 脚本运行出错？**
```bash
scripts\clean.bat   # 清理
scripts\install.bat # 重装
```

**Q: 如何生成离线版PDF？**
```bash
# 需要先安装 Calibre: https://calibre-ebook.com/
scripts\pdf.bat
```

---

## 📞 获取帮助

- 详细说明：查看 `scripts\README.md`
- 项目指南：查看 `使用指南.md`
- 技术交流：微信 **wlxklyh**

---

## ⚡ Pro Tips

1. **创建桌面快捷方式**：右键 `开始.bat` → 发送到 → 桌面快捷方式
2. **自定义菜单**：编辑 `scripts\menu.bat` 添加自己的命令
3. **批量操作**：可以串联多个脚本使用
4. **查看日志**：脚本执行时会显示详细的操作步骤

---

**💪 现在就开始使用吧！**

双击 `开始.bat` 启动快速菜单！

